const TableCheck = () => {
    return(
        <div>Table XYZ Check</div>
    )
}

export default TableCheck;

// How will I have the check display base on link from table?
    // <Link to="/menu-tab" state={{ from: "someStateFromTableMap" }}>
    // import { useLocation } from 'react-router-dom'
        // const location = useLocation()
        // const { from } = location.state

// Things to display from table

// Things to add / update

// Reason to Set to empty?